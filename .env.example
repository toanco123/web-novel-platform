# Copy file này thành .env rồi sửa giá trị (.env không được commit)

# Dev server: đổi cổng/host tùy ý, vd PORT=4000
HOST=localhost
PORT=3000

# Supabase: lấy từ Supabase Dashboard > Project Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
